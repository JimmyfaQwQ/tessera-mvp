$template Worker() {
    expose result: int;

    async function __on_terminate__(): void {}
}

$Worker() {
    let result: int = 99;
} := handle;

handle.terminate().wait();

let v: int = handle.result;
assert(v == 99);
