[package]
name = "tessera-lint"
version = "0.1.0"
edition = "2021"

[dependencies]
tessera-ast   = { path = "../tessera-ast" }
tessera-types = { path = "../tessera-types" }
miette        = { workspace = true }
