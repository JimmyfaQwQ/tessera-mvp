let x: int = 10;
let y: int = 20;
let z: int = x + y * 2;
println(z);

let msg: String = "hello";
println(msg);

let flag: bool = z > 30;
if (flag) {
    println("big");
} else {
    println("small");
}
